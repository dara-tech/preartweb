#!/usr/bin/expect -f
set timeout 60

spawn ssh root@172.104.33.79
expect "password:"
send "Mpi@2025\r"
expect "root@localhost"

send "lxc exec nodejs-web -- bash -c \"cat /root/artweb/backend/.env && echo === && cat /tmp/backend_env_bak 2>/dev/null && echo CHECK_DONE\"\r"
expect "CHECK_DONE"

send "exit\r"
expect eof
